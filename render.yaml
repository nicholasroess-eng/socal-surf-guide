services:
  - type: web
    name: socal-surf-guide
    runtime: ruby
    plan: free
    buildCommand: gem install bundler -v 2.5.23 && bundle install
    startCommand: bundle exec ruby server.rb
    healthCheckPath: /
    envVars:
      - key: RUBY_VERSION
        value: 3.2.2
